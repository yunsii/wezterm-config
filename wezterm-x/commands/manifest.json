[
  {
    "id": "clipboard.copy-or-sigint",
    "label": "Clipboard: Copy selection or send SIGINT",
    "description": "With a terminal selection, copy to the system clipboard and clear; otherwise send a normal `Ctrl+c` to the pane.",
    "scope": "clipboard",
    "context": "any",
    "binding": {"handler": "clipboard.copy_or_sigint"},
    "hotkeys": [
      { "keys": "Ctrl+c", "layer": "wezterm" }
    ]
  },
  {
    "id": "clipboard.copy-selection-strict",
    "label": "Clipboard: Copy selection (strict)",
    "description": "With a terminal selection, copy and clear; otherwise forward `Ctrl+Shift+c` to the pane.",
    "scope": "clipboard",
    "context": "any",
    "binding": {"handler": "clipboard.copy_selection_strict"},
    "hotkeys": [
      { "keys": "Ctrl+Shift+C", "layer": "wezterm" }
    ]
  },
  {
    "id": "clipboard.paste-smart",
    "label": "Clipboard: Paste (smart)",
    "description": "In hybrid-wsl ask the Windows native helper for the live clipboard at paste time; fall back to plain clipboard paste; paste exported WSL image path for images.",
    "scope": "clipboard",
    "context": "any",
    "binding": {"handler": "clipboard.paste_smart"},
    "hotkeys": [
      { "keys": "Ctrl+v", "layer": "wezterm" }
    ]
  },
  {
    "id": "clipboard.paste-plain",
    "label": "Clipboard: Paste (plain)",
    "description": "Force a normal clipboard paste without the image-export helper.",
    "scope": "clipboard",
    "context": "any",
    "binding": {"handler": "clipboard.paste_plain"},
    "hotkeys": [
      { "keys": "Ctrl+Shift+V", "layer": "wezterm" }
    ]
  },
  {
    "id": "link.open-in-viewport",
    "label": "Link: Open URL from viewport",
    "description": "Trigger WezTerm QuickSelect to label every `http(s)://…` URL across the visible pane and WezTerm scrollback; press the label key to open the URL via the system default handler. Keyboard-first counterpart to `Ctrl+LeftClick`.",
    "scope": "clipboard",
    "context": "any",
    "binding": {"handler": "link.open_in_viewport"},
    "hotkeys": [
      { "keys": "Alt+l", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "pane.split-vertical",
    "label": "Pane: Split vertically",
    "description": "Create a top/bottom tmux split in the current pane.",
    "scope": "commands-and-splits",
    "context": "tmux-backed",
    "binding": {"kind": "tmux-chord-leaf", "table": "command-chord", "exec": "split-window -v -c \"#{pane_current_path}\""},
    "hotkeys": [
      { "keys": "Ctrl+k v", "layer": "tmux-chord" }
    ],
    "palette": {
      "accelerator": "v",
      "command": ["bash", "-lc", "tmux split-window -v -c \"$PWD\""],
      "success_message": "Created a vertical split.",
      "failure_message": "Failed to create a vertical split."
    }
  },
  {
    "id": "pane.split-horizontal",
    "label": "Pane: Split horizontally",
    "description": "Create a left/right tmux split in the current pane.",
    "scope": "commands-and-splits",
    "context": "tmux-backed",
    "binding": {"kind": "tmux-chord-leaf", "table": "command-chord", "exec": "split-window -h -c \"#{pane_current_path}\""},
    "hotkeys": [
      { "keys": "Ctrl+k h", "layer": "tmux-chord" }
    ],
    "palette": {
      "accelerator": "h",
      "command": ["bash", "-lc", "tmux split-window -h -c \"$PWD\""],
      "success_message": "Created a horizontal split.",
      "failure_message": "Failed to create a horizontal split."
    }
  },
  {
    "id": "pane.rotate-next",
    "label": "Pane: Rotate focus to next",
    "description": "Rotate focus to the next pane in the current tmux window (same semantics as tmux `prefix o`).",
    "scope": "window-and-pane-navigation",
    "context": "tmux-backed",
    "binding": {"handler": "pane.rotate_next"},
    "hotkeys": [
      { "keys": "Alt+o", "layer": "wezterm" }
    ],
    "palette": {
      "command": ["bash", "-lc", "tmux select-pane -t \"${COMMAND_PANEL_WINDOW_ID}.+\""],
      "success_message": "Rotated to next pane."
    }
  },
  {
    "id": "pane.close-current",
    "label": "Pane: Close current",
    "description": "Run tmux `kill-pane` on the focused pane; if it is the last pane in the window, tmux closes the window too.",
    "scope": "window-and-pane-navigation",
    "context": "tmux-backed",
    "binding": {"kind": "tmux-chord-leaf", "table": "command-chord", "exec": "kill-pane"},
    "hotkeys": [
      { "keys": "Ctrl+k x", "layer": "tmux-chord" }
    ],
    "palette": {
      "command": ["bash", "-lc", "tmux kill-pane -t \"$COMMAND_PANEL_WINDOW_ID\""]
    }
  },
  {
    "id": "tab.next",
    "label": "Tab: Next",
    "description": "Activate the next WezTerm tab in the current workspace.",
    "scope": "window-and-pane-navigation",
    "context": "any",
    "binding": {"handler": "tabs.activate_relative", "args": {"delta": 1}},
    "hotkeys": [
      { "keys": "Alt+n", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "tab.previous",
    "label": "Tab: Previous",
    "description": "Activate the previous WezTerm tab in the current workspace.",
    "scope": "window-and-pane-navigation",
    "context": "any",
    "binding": {"handler": "tabs.activate_relative", "args": {"delta": -1}},
    "hotkeys": [
      { "keys": "Alt+Shift+N", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "tab.overflow-picker",
    "label": "Tab: Open overflow picker",
    "description": "Open the cross-workspace overflow picker (tmux display-popup running native/picker/bin/picker overflow) listing every configured session across every workspace whose items snapshot has been written. Each row is tagged ● visible / ◐ warm / ○ cold and the active workspace's rows rank first; the always-on substring filter matches workspace + label + cwd, and Tab toggles between all-workspaces and current-workspace-only scope. Selection routes through tab-overflow-dispatch.sh — visible activates the existing tab, warm switches the overflow pane to that session, cold falls back to managed-spawn. When the picked row's workspace isn't the foregrounded one, lua's tab.* event handlers issue a SwitchToWorkspace so the gui follows. Falls back to a single-workspace tmux display-menu when the Go picker binary is missing. See docs/tab-visibility.md.",
    "scope": "window-and-pane-navigation",
    "context": "tmux-backed",
    "binding": {"handler": "tabs.overflow_picker"},
    "hotkeys": [
      { "keys": "Alt+x", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "tab.select-by-index",
    "label": "Tab: Jump by index",
    "description": "`Alt+N` activates the WezTerm tab at position N (1-9) in the current workspace.",
    "scope": "window-and-pane-navigation",
    "context": "any",
    "args_schema": { "kind": "integer", "range": [1, 9] },
    "binding": {"handler": "tabs.activate_by_index"},
    "hotkeys": [
      { "keys": "Alt+1", "layer": "wezterm", "args": 1 },
      { "keys": "Alt+2", "layer": "wezterm", "args": 2 },
      { "keys": "Alt+3", "layer": "wezterm", "args": 3 },
      { "keys": "Alt+4", "layer": "wezterm", "args": 4 },
      { "keys": "Alt+5", "layer": "wezterm", "args": 5 },
      { "keys": "Alt+6", "layer": "wezterm", "args": 6 },
      { "keys": "Alt+7", "layer": "wezterm", "args": 7 },
      { "keys": "Alt+8", "layer": "wezterm", "args": 8 },
      { "keys": "Alt+9", "layer": "wezterm", "args": 9 }
    ],
    "hotkey_display": "Alt+1..9",
    "palette": { "display_only": true }
  },
  {
    "id": "agent.new-conversation",
    "label": "Agent CLI: Start a new conversation (/new)",
    "description": "When the current pane's foreground process is a known agent CLI (claude / codex), type `/new` followed by Enter to start a fresh conversation. Otherwise forwards `Ctrl+n` to the pane unchanged.",
    "scope": "project-navigation",
    "context": "any",
    "binding": {"handler": "agent.new_conversation"},
    "hotkeys": [
      { "keys": "Ctrl+n", "layer": "wezterm" }
    ],
    "palette": {
      "command": ["bash", "-lc", "proc=$(tmux display-message -p -t \"$COMMAND_PANEL_WINDOW_ID\" '#{pane_current_command}'); case \"$proc\" in claude*|codex*) tmux send-keys -t \"$COMMAND_PANEL_WINDOW_ID\" '/new' Enter ;; *) tmux send-keys -t \"$COMMAND_PANEL_WINDOW_ID\" C-n ;; esac"]
    }
  },
  {
    "id": "git.push-current",
    "label": "Git: Push current branch (agent-aware)",
    "description": "Run `git push` in the focused pane. When the foreground process is a known agent CLI (claude / codex), prefix with `!` (with a tiny inter-key gap so the shell-escape detector fires) so the agent runs it as an inline shell command instead of treating it as chat input; otherwise the literal `git push` + Enter is sent to the shell. Note: capturing `Ctrl+P` at the WezTerm layer pre-empts readline previous-history / vim / TUIs in any pane — remap if you want those back. In tmux-backed panes WezTerm forwards `\\x1b[20102~` (User3) and tmux's root binding does the foreground check via `pane_current_command`; outside tmux WezTerm checks `get_foreground_process_name()` directly.",
    "scope": "project-navigation",
    "context": "any",
    "binding": {"handler": "git.push_current"},
    "hotkeys": [
      { "keys": "Ctrl+P", "layer": "wezterm" }
    ],
    "palette": {
      "command": ["bash", "-lc", "proc=$(tmux display-message -p -t \"$COMMAND_PANEL_WINDOW_ID\" '#{pane_current_command}'); case \"$proc\" in claude*|codex*) bash '{repo_root}/scripts/runtime/git-push-into-pane.sh' agent \"$COMMAND_PANEL_WINDOW_ID\" ;; *) bash '{repo_root}/scripts/runtime/git-push-into-pane.sh' shell \"$COMMAND_PANEL_WINDOW_ID\" ;; esac"]
    }
  },
  {
    "id": "command-palette.chord-prefix",
    "label": "Command chord: Activate prefix",
    "description": "Forward `Ctrl+k` to a tmux-backed pane so the follow-up chord keys (`v` / `h` / `x` / `g`) resolve in the tmux `command-chord` table; toast when the current pane is not running tmux.",
    "scope": "commands-and-splits",
    "context": "tmux-backed",
    "binding": {"handler": "command_palette.chord_prefix"},
    "hotkeys": [
      { "keys": "Ctrl+k", "layer": "wezterm" }
    ]
  },
  {
    "id": "command-palette.open",
    "label": "Command palette: Open",
    "description": "Open the tmux-owned searchable command palette (falls back to WezTerm native palette outside tmux).",
    "scope": "commands-and-splits",
    "context": "any",
    "binding": {"handler": "command_palette.open"},
    "hotkeys": [
      { "keys": "Ctrl+Shift+P", "layer": "wezterm" }
    ]
  },
  {
    "id": "command-palette.open-native",
    "label": "Command palette: Open WezTerm native",
    "description": "Open WezTerm's built-in command palette directly.",
    "scope": "commands-and-splits",
    "context": "any",
    "binding": {"handler": "command_palette.open_native"},
    "hotkeys": [
      { "keys": "Ctrl+Shift+;", "layer": "wezterm" },
      { "keys": "Ctrl+Shift+:", "layer": "wezterm" }
    ]
  },
  {
    "id": "links.picker",
    "label": "Links: Open picker",
    "description": "Resolve project links via the `vscode-links` CLI for the current pane's cwd and surface them in the native Go picker. Enter opens the URL on the Windows host; Ctrl+Y copies it. Requires `vscode-links` (install via the project's install.sh, or set VSCODE_LINKS_BIN) plus `jq` on PATH.",
    "scope": "project-navigation",
    "context": "tmux-backed",
    "binding": {
      "kind": "tmux-chord-leaf",
      "table": "command-chord",
      "exec": "run-shell -b \"bash #{@wezterm_runtime_root}/scripts/runtime/links-menu.sh '#{pane_current_path}'\""
    },
    "hotkeys": [
      { "keys": "Ctrl+k l", "layer": "tmux-chord" }
    ],
    "palette": {
      "accelerator": "l",
      "command": ["bash", "-lc", "tmux display-popup -E -w 80% -h 60% \"bash $WEZTERM_RUNTIME_ROOT/scripts/runtime/links-menu.sh '$PWD'\""],
      "success_message": "Opened links picker.",
      "failure_message": "Failed to open links picker."
    }
  },
  {
    "id": "vscode.open-current-dir",
    "label": "VS Code: Open current directory",
    "description": "In tmux-backed panes forward to tmux; in hybrid-wsl outside tmux open the worktree root on the Windows host.",
    "scope": "project-navigation",
    "context": "any",
    "binding": {"handler": "vscode.open_current_dir"},
    "hotkeys": [
      { "keys": "Alt+v", "layer": "wezterm" }
    ],
    "palette": {
      "command": ["bash", "-lc", "bash {repo_root}/scripts/runtime/open-current-dir-in-vscode.sh --window \"$COMMAND_PANEL_WINDOW_ID\" -- \"$COMMAND_PANEL_CWD\""]
    }
  },
  {
    "id": "vscode.force-close-all-windows",
    "label": "VS Code: Force close all windows",
    "description": "Run `taskkill /IM code.exe /F` on the Windows host.",
    "scope": "commands-and-splits",
    "context": "hybrid-wsl",
    "hotkeys": [],
    "palette": {
      "accelerator": "k",
      "command": ["cmd.exe", "/c", "taskkill", "/IM", "code.exe", "/F"],
      "confirm_message": "Force close all VS Code windows? Unsaved editor state will be lost.",
      "success_message": "Closed all VS Code windows.",
      "failure_message": "Failed to close VS Code windows."
    }
  },
  {
    "id": "worktree.picker",
    "label": "Worktree: Pick in this repo family",
    "description": "Open a centered tmux popup worktree picker; selecting an unopened worktree creates its tmux window on demand and launches the agent under the `<base>-resume` profile.",
    "scope": "project-navigation",
    "context": "tmux-backed",
    "binding": {"handler": "worktree.picker"},
    "hotkeys": [
      { "keys": "Alt+g", "layer": "wezterm" }
    ],
    "palette": {
      "command": ["bash", "-lc", "bash {repo_root}/scripts/runtime/tmux-worktree-menu.sh \"$COMMAND_PANEL_SESSION_NAME\" \"$COMMAND_PANEL_WINDOW_ID\" \"$COMMAND_PANEL_CWD\""]
    }
  },
  {
    "id": "worktree.cycle-next",
    "label": "Worktree: Cycle to next",
    "description": "Cycle to the next git worktree in the current repo family, creating its tmux window on demand and launching the agent under the `<base>-resume` profile.",
    "scope": "project-navigation",
    "context": "tmux-backed",
    "binding": {"handler": "worktree.cycle_next"},
    "hotkeys": [
      { "keys": "Alt+Shift+G", "layer": "wezterm" }
    ],
    "palette": {
      "command": ["bash", "-lc", "bash {repo_root}/scripts/runtime/tmux-worktree-cycle.sh \"$COMMAND_PANEL_SESSION_NAME\" \"$COMMAND_PANEL_WINDOW_ID\" \"$COMMAND_PANEL_CWD\""]
    }
  },
  {
    "id": "worktree.quick-create-dev",
    "label": "Worktree: Quick-create dev-* (long-lived)",
    "description": "Open a `dev-:` prompt; entered name becomes worktree `dev-<slug>` for long-lived parallel development. Agent launches under the `<base>-resume` profile (auto-continue cwd's last conversation, fall back to fresh on first open).",
    "scope": "project-navigation",
    "context": "tmux-backed",
    "binding": {"kind": "tmux-chord-leaf", "table": "worktree-chord", "switch_first": true, "exec": "command-prompt -p \"dev-:\" \"run-shell -b 'bash #{@wezterm_runtime_root}/scripts/runtime/worktree/open-task-window --type dev \\\"%1\\\" >/dev/null 2>&1'\""},
    "hotkeys": [
      { "keys": "Ctrl+k g d", "layer": "tmux-chord" }
    ],
    "palette": {
      "accelerator": "d",
      "command": [
        "bash",
        "-lc",
        "tmux command-prompt -p 'dev-:' \"run-shell -b 'bash {repo_root}/scripts/runtime/worktree/open-task-window --type dev \\\"%1\\\" >/dev/null 2>&1'\""
      ],
      "success_message": "Opened dev-: prompt.",
      "failure_message": "Failed to open dev prompt."
    }
  },
  {
    "id": "worktree.quick-create-task",
    "label": "Worktree: Quick-create task-* (short-lived)",
    "description": "Open a `task-:` prompt; entered name becomes worktree `task-<slug>` for PR-scoped focused work. Agent launches under the `<base>-resume` profile.",
    "scope": "project-navigation",
    "context": "tmux-backed",
    "binding": {"kind": "tmux-chord-leaf", "table": "worktree-chord", "switch_first": true, "exec": "command-prompt -p \"task-:\" \"run-shell -b 'bash #{@wezterm_runtime_root}/scripts/runtime/worktree/open-task-window --type task \\\"%1\\\" >/dev/null 2>&1'\""},
    "hotkeys": [
      { "keys": "Ctrl+k g t", "layer": "tmux-chord" }
    ],
    "palette": {
      "accelerator": "t",
      "command": [
        "bash",
        "-lc",
        "tmux command-prompt -p 'task-:' \"run-shell -b 'bash {repo_root}/scripts/runtime/worktree/open-task-window --type task \\\"%1\\\" >/dev/null 2>&1'\""
      ],
      "success_message": "Opened task-: prompt.",
      "failure_message": "Failed to open task prompt."
    }
  },
  {
    "id": "worktree.quick-create-hotfix",
    "label": "Worktree: Quick-create hotfix-* (urgent)",
    "description": "Open a `hotfix-:` prompt; entered name becomes worktree `hotfix-<slug>` for urgent fixes. Agent launches under the `<base>-resume` profile; on first open of a brand-new worktree the resume command falls back to a fresh agent automatically.",
    "scope": "project-navigation",
    "context": "tmux-backed",
    "binding": {"kind": "tmux-chord-leaf", "table": "worktree-chord", "switch_first": true, "exec": "command-prompt -p \"hotfix-:\" \"run-shell -b 'bash #{@wezterm_runtime_root}/scripts/runtime/worktree/open-task-window --type hotfix \\\"%1\\\" >/dev/null 2>&1'\""},
    "hotkeys": [
      { "keys": "Ctrl+k g h", "layer": "tmux-chord" }
    ],
    "palette": {
      "accelerator": "h",
      "command": [
        "bash",
        "-lc",
        "tmux command-prompt -p 'hotfix-:' \"run-shell -b 'bash {repo_root}/scripts/runtime/worktree/open-task-window --type hotfix \\\"%1\\\" >/dev/null 2>&1'\""
      ],
      "success_message": "Opened hotfix-: prompt.",
      "failure_message": "Failed to open hotfix prompt."
    }
  },
  {
    "id": "worktree.reclaim-current",
    "label": "Worktree: Reclaim current task-/hotfix- worktree",
    "description": "Reclaim the current pane's worktree after merge. Refuses on main, dev-*, dirty/untracked changes, or unmerged branch. Switches focus to the main worktree window, runs reclaim detached, then closes the original window.",
    "scope": "project-navigation",
    "context": "tmux-backed",
    "binding": {"kind": "tmux-chord-leaf", "table": "worktree-chord", "switch_first": true, "exec": "run-shell -b \"bash #{@wezterm_runtime_root}/scripts/runtime/worktree/reclaim-current-window #{q:pane_current_path} #{pane_id}\""},
    "hotkeys": [
      { "keys": "Ctrl+k g r", "layer": "tmux-chord" }
    ],
    "palette": {
      "accelerator": "r",
      "command": [
        "bash",
        "-lc",
        "{repo_root}/scripts/runtime/worktree/reclaim-current-window"
      ],
      "success_message": "Reclaim dispatched.",
      "failure_message": "Reclaim refused (see message)."
    }
  },
  {
    "id": "chrome.open-debug-profile",
    "label": "Chrome: Open debug profile",
    "description": "Open the configured Chrome debug browser profile from `wezterm-x/local/constants.lua` via the Windows native helper.",
    "scope": "project-navigation",
    "context": "hybrid-wsl",
    "binding": {"handler": "chrome.open_debug_profile", "args": {"headless": true}},
    "hotkeys": [
      { "keys": "Alt+b", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "chrome.open-debug-profile-visible",
    "label": "Chrome: Open debug profile (visible)",
    "description": "Open or reset the Chrome debug browser profile in visible (headful) mode for manual verification; shares the debug `user_data_dir` and port with the headless instance.",
    "scope": "project-navigation",
    "context": "hybrid-wsl",
    "binding": {"handler": "chrome.open_debug_profile", "args": {"headless": false}},
    "hotkeys": [
      { "keys": "Alt+Shift+B", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "attention.jump-waiting",
    "label": "Attention: Jump to next waiting task",
    "description": "Jump to the next agent pane whose status is `waiting`. Repeated presses cycle past the current target; silent when nothing is waiting. Toasts outside tmux.",
    "scope": "project-navigation",
    "context": "tmux-backed",
    "binding": {"handler": "attention.jump_waiting"},
    "hotkeys": [
      { "keys": "Alt+,", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "attention.jump-done",
    "label": "Attention: Jump to next done task",
    "description": "Jump to the next agent pane whose status is `done`; the entry is auto-cleared as the jump lands. Silent when nothing is done.",
    "scope": "project-navigation",
    "context": "tmux-backed",
    "binding": {"handler": "attention.jump_done"},
    "hotkeys": [
      { "keys": "Alt+.", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "attention.overlay",
    "label": "Attention: Open pending-task overlay",
    "description": "Open a centered tmux popup picker listing every pending agent task across workspaces. Up/Down to move, `Enter` to jump; pick the trailing `clear all` sentinel to wipe state.json. Press `Alt+/` again (or `Esc`) to close — the popup owns the chord while open, so the same key opens and closes it.",
    "scope": "project-navigation",
    "context": "tmux-backed",
    "binding": {"handler": "attention.overlay"},
    "hotkeys": [
      { "keys": "Alt+/", "layer": "wezterm" }
    ],
    "palette": {
      "command": ["bash", "-lc", "bash {repo_root}/scripts/runtime/tmux-attention-menu.sh"]
    }
  },
  {
    "id": "session.refresh-current-window",
    "label": "Session: Refresh current window",
    "description": "Respawn only the focused tmux window in place.",
    "scope": "session-maintenance",
    "context": "tmux-backed",
    "hotkeys": [],
    "palette": {
      "accelerator": "r",
      "command": ["bash", "{repo_root}/scripts/runtime/tmux-reset.sh", "refresh-current-window"],
      "success_message": "Refreshed current tmux window.",
      "failure_message": "Failed to refresh current tmux window."
    }
  },
  {
    "id": "session.refresh-current-session",
    "label": "Session: Refresh current session",
    "description": "Rebuild the attached tmux session via replacement session.",
    "scope": "session-maintenance",
    "context": "tmux-backed",
    "hotkeys": [],
    "palette": {
      "accelerator": "s",
      "command": ["bash", "{repo_root}/scripts/runtime/tmux-reset.sh", "refresh-current-session"],
      "success_message": "Refreshed current tmux session.",
      "failure_message": "Failed to refresh current tmux session."
    }
  },
  {
    "id": "session.refresh-current-workspace",
    "label": "Session: Refresh current workspace",
    "description": "Rebuild every tmux session that belongs to this workspace.",
    "scope": "session-maintenance",
    "context": "tmux-backed",
    "hotkeys": [],
    "palette": {
      "accelerator": "w",
      "command": ["bash", "{repo_root}/scripts/runtime/tmux-reset.sh", "refresh-current-workspace"],
      "confirm_message": "Refresh every tmux session in the current workspace?",
      "success_message": "Refreshed current workspace sessions.",
      "failure_message": "Failed to refresh current workspace sessions."
    }
  },
  {
    "id": "session.refresh-all-sessions",
    "label": "Session: Refresh all sessions",
    "description": "Rebuild every tmux session on the current tmux server.",
    "scope": "session-maintenance",
    "context": "tmux-backed",
    "hotkeys": [],
    "palette": {
      "accelerator": "a",
      "command": ["bash", "{repo_root}/scripts/runtime/tmux-reset.sh", "refresh-all"],
      "confirm_message": "Refresh every tmux session on this tmux server?",
      "success_message": "Refreshed all tmux sessions.",
      "failure_message": "Failed to refresh all tmux sessions."
    }
  },
  {
    "id": "workspace.switch-default",
    "label": "Workspace: Switch to default",
    "description": "Switch to the built-in WezTerm `default` workspace.",
    "scope": "workspaces",
    "context": "any",
    "binding": {"handler": "workspace.switch", "args": {"name": "default"}},
    "hotkeys": [
      { "keys": "Alt+d", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "workspace.switch-work",
    "label": "Workspace: Switch to work",
    "description": "Open or switch to `work` using directories configured in `wezterm-x/local/workspaces.lua`.",
    "scope": "workspaces",
    "context": "any",
    "binding": {"handler": "workspace.switch", "args": {"name": "work"}},
    "hotkeys": [
      { "keys": "Alt+w", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "workspace.switch-config",
    "label": "Workspace: Switch to config",
    "description": "Open or switch to the `config` workspace.",
    "scope": "workspaces",
    "context": "any",
    "binding": {"handler": "workspace.switch", "args": {"name": "config"}},
    "hotkeys": [
      { "keys": "Alt+c", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "workspace.switch-opensource",
    "label": "Workspace: Switch to opensource",
    "description": "Open or switch to `opensource` using directories configured in `wezterm-x/local/workspaces.lua` (intended for ~/github open-source projects).",
    "scope": "workspaces",
    "context": "any",
    "binding": {"handler": "workspace.switch", "args": {"name": "opensource"}},
    "hotkeys": [
      { "keys": "Alt+s", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "workspace.switch-mock-deck",
    "label": "Workspace: Switch to mock-deck (demo)",
    "description": "Switch to the `mock-deck` demo workspace (six tabs simulating multi-agent activity). Only useful when the `mock-deck` workspace is registered in `wezterm-x/local/workspaces.lua` and the orchestrator at `scripts/dev/mock-deck/mock-deck.sh` is running.",
    "scope": "workspaces",
    "context": "any",
    "binding": {"handler": "workspace.switch", "args": {"name": "mock-deck"}},
    "hotkeys": [
      { "keys": "Alt+m", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "workspace.cycle-next",
    "label": "Workspace: Cycle to next",
    "description": "Rotate through all currently known workspaces.",
    "scope": "workspaces",
    "context": "any",
    "binding": {"handler": "workspace.cycle_next"},
    "hotkeys": [
      { "keys": "Alt+p", "layer": "wezterm" }
    ],
    "palette": { "display_only": true }
  },
  {
    "id": "workspace.close-current",
    "label": "Workspace: Close current",
    "description": "Open a centered WezTerm confirmation overlay to close the current non-default workspace.",
    "scope": "workspaces",
    "context": "any",
    "binding": {"handler": "workspace.close_current"},
    "hotkeys": [
      { "keys": "Alt+Shift+X", "layer": "wezterm" }
    ]
  },
  {
    "id": "app.quit",
    "label": "App: Quit WezTerm",
    "description": "Quit WezTerm and close all windows; WezTerm handles its own confirmation.",
    "scope": "workspaces",
    "context": "any",
    "binding": {"handler": "app.quit"},
    "hotkeys": [
      { "keys": "Alt+Shift+Q", "layer": "wezterm" }
    ]
  }
]
