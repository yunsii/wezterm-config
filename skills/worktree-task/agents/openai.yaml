interface:
  display_name: "Worktree Task"
  short_description: "Create or reclaim linked task worktrees with optional runtime provider launch."
  default_prompt: "Use this skill to create or reclaim a linked git task worktree, optionally launch it through a built-in or custom provider such as tmux-agent, and clean up runtime state and merged branches safely during reclaim."
